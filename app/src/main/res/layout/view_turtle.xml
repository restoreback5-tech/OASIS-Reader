<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Rama (Fondo superior) -->
    <ImageView
        android:id="@+id/branch_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:src="@drawable/branch_large"
        android:adjustViewBounds="true" />

    <!-- Hoja que cae (Animación) -->
    <ImageView
        android:id="@+id/leaf_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="top|center_horizontal"
        android:src="@drawable/leaf_single"
        android:visibility="gone" />

    <!-- Tortuga (Parte inferior izquierda) -->
    <ImageView
        android:id="@+id/turtle_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="16dp"
        android:src="@drawable/totu_idle" />

</FrameLayout>
